# R workflow

The R workflow contains the thesis ODE models.

- Put executable analysis scripts in `scripts/`.
- Put reusable R functions in `R/`.
- Put model parameters in `config/`.
- Read inputs from `../../data/raw/` and write generated tables and figures to `../../results/`.
